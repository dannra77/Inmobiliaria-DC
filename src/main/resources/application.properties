# Configuración por defecto - DESARROLLO LOCAL
spring.application.name=inmobiliaria-dc
server.port=8080

# MySQL Local con WampServer
spring.datasource.url=jdbc:mysql://localhost:3306/inmobiliaria_dc?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Thymeleaf
spring.thymeleaf.cache=false

# Security
spring.security.user.name=admin
spring.security.user.password=dc2024